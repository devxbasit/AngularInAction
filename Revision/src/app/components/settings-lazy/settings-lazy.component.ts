import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-settings-lazy',
  standalone: true,
  imports: [RouterOutlet],
  templateUrl: './settings-lazy.component.html',
  styleUrl: './settings-lazy.component.scss'
})
export class SettingsLazyComponent {

}
